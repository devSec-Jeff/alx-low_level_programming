#include "main.h"

/**
 * _strcat - concatenate two strings
 * @dest: the first character string pointer
 * @src: the second character string pointer
 * Return: a pointer to the resulting string dest
 */
char *_strcat(char *dest, char *src)
{
	dest = dest + src;
	return (dest);
}
