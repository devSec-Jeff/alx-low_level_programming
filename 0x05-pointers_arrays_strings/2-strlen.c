#include "main.h"

/**
 * _strlen - function returning lenght of a string
 * @s: character string
 * Return: length of a string (int)
 */
int _strlen(char *s)
{
	int len;

	len = sizeof(s);
	return (--len);
}
